<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Task</title>
    <link rel="stylesheet" href="stylesheets/addtask.css">
</head>
<body>
     <div id="main">
        <form action="/addtask" method="post">
            <div class="field">
                <div class="field__head">
                  <label class="field__ttl" for="text">Enter title and description of your task</label>
                  <span class="field__tag is-require">Required</span>
                </div>
                <div class="field__body">
                  <div class="text-box">
                    <input name="title" id="title" type="text" placeholder="Enter text" required>
                  </div>
                  <div class="textarea-box">
                    <textarea name="desc" id="desc" placeholder="Enter message"></textarea>
                  </div>
                </div>
              </div>
         <button type="submit">Submit</button>

         </div>
        </form>


</body>
</html>

