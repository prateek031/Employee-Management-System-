<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Today's Update</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
        }

        .nav {
            background-color: #3498db;
            padding: 1em;
            width: 100%;
            margin-bottom: 1em;
        }

        .nav h1 {
            color: #fff;
            font-size: 1.5em;
        }

        .assigner {
            width: 100%;
            display: flex;
            justify-content: space-between;
            padding: 1em;
            align-items: center;
        }

        #circle {
            height: 50px;
            width: 50px;
            background-color: #e74c3c;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-right: 1em;
        }

        #circle i {
            color: #fff;
            font-size: 1.5em;
        }

        .asleft h2 {
            color: #333;
            font-size: 1.2em;
            margin-bottom: 0.5em;
        }

        .asright form {
            margin: 0.5em;
        }

        button {
            background-color: #2ecc71;
            color: #fff;
            padding: 0.5em 1em;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #27ae60;
        }

        p {
            font-size: 1.2em;
            margin-bottom: 1em;
        }

        .tslist {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 1em;
        }

        #task {
            width: 100%;
            max-width: 400px;
            padding: 1em;
            border-radius: 8px;
            background-color: #fff;
            margin-bottom: 1em;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            text-overflow: ellipsis;
            display: flex;
            flex-direction: column;
            align-items: baseline;
        }

        #task h2 {
            color: #333;
            font-size: 1.5em;
            margin-bottom: 0.5em;
        }

        #task p {
            color: #666;
            font-size: 1.1em;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }
    </style>
</head>

<body>
    <div class="nav">
        <h1>Add your updates for today..</h1>
    </div>

    <div class="assigner">
        <div id="circle">
            <i class="ri-arrow-left-s-line"></i>
        </div>
        <div class="asleft">
            <h2>Please give one SOD & EOD in a day </h2>
        </div>
        <div class="asright">
            <form action="/addupdate" method="get">
                <button type="submit">Add Update</button>
            </form>
        </div>
    </div>

    <p>Welcome to <strong><%= user.name %></strong> Profile</p>

    <div class="tslist">
        <% const today = new Date();
        const currentDate = today.toISOString().slice(0, 10);
        const filteredUpdates = updates.filter(update => {
            const updateDate = new Date(update.createdAt).toISOString().slice(0, 10);
            return updateDate === currentDate;
        });
        filteredUpdates.sort((a, b) => b.createdAt - a.createdAt);
        filteredUpdates.forEach(function (update) { %>
        <div id="task">
            <h2><%= update.title %></h2>
            <p><%= update.desc %></p>
            <p>Date: <%= update.createdAt.toLocaleDateString('en-GB') %></p>
        </div>
        <% }) %>
    </div>
    
</body>
<script>
  const urlParams = new URLSearchParams(window.location.search);
  const success = urlParams.get('success');
  if (success) {
      alert('Your update is recorded successfully!');
  }
  var nav = document.querySelector("#circle");
  nav.addEventListener("click", function () {
    window.open("/dashboard", "_self");
});
</script>

</html>
